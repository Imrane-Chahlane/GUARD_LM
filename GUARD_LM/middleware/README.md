Next.js requires runtime middleware in the repository root as `middleware.ts`.

This folder is reserved for future shared middleware helpers, such as tenant resolution,
rate limiting, or request audit utilities.
